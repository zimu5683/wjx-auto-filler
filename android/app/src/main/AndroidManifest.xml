<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 纯接口填表：只需要网络；不打开浏览器、不模拟页面操作 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- 相机实时扫码（相册导入走系统照片选择器，不需要存储权限） -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- 应用内自动更新：下载 APK 后拉起系统安装器 -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 配置含姓名/学号等个人信息：禁止云备份与 adb backup 带走（契约 §4.4 隐私节） -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.WjxAutofill"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 实时扫码页：CameraX 预览 + zxing 解码；相机权限只在进入本页时申请 -->
        <activity
            android:name=".qr.QrScanActivity"
            android:exported="false"
            android:label="@string/scan_title" />

        <!--
            人机验证兜底页：WebView 加载真实问卷 URL，注入页面自身的 loadCaptchShow()
            让用户完成平台验证。不填表、不点提交，只收割 captchaVerifyParam。
        -->
        <activity
            android:name=".ui.CaptchaActivity"
            android:exported="false"
            android:label="@string/captcha_title" />

        <!-- 应用内更新：把 cacheDir/updates 下的 APK 通过 content:// 交给系统安装器 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
